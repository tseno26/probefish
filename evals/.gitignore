# Benchmark output (regenerated by run-benchmark.ps1 / run-benchmark.sh).
results/*.json
results/*.ndjson

# node_modules under evals/ is already covered by the repo-root .gitignore
# (node_modules/ matches at any depth), listed here too for clarity.
fixture/node_modules/
oracle/node_modules/
