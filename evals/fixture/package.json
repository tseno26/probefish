{
  "name": "probefish-eval-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Trap fixture for the probefish A/B eval harness. Not a real app.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
