Read README.md in the current directory for context, then complete the task described in its TASK section: consolidate the duplicated code in src/formatUser.ts and src/profile.ts into single, well-tested implementations without changing observable behavior for any existing caller, keeping every existing exported function name and every existing test in test/ passing. When you are confident the consolidation is complete and correct, stop.
